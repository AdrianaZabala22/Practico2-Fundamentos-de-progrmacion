#include <stdio.h>
#include <math.h>
int main() {
    double x, suma = 0;
    int n, i;
    printf("X = "); scanf("%lf", &x);
    printf("n = "); scanf("%d", &n);
    for(i = 1; i <= n; i++) {
        suma += pow(x, i);
    }
    printf("S = %.4lf\n", suma);
    return 0;
}
