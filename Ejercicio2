#include <stdio.h>
int main() {
    int n, i;
    long long suma = 0;
    printf("Ingrese N: ");
    scanf("%d", &n);
    for(i = 1; i <= n; i++) {
        suma += i * i;
    }
    printf("Suma de cuadrados = %lld\n", suma);
    return 0;
}
